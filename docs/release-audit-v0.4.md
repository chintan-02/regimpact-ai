# v0.4 alpha release audit

Scope: controlled regulatory-impact workflows, versioned evidence grounding, deterministic safety
gates, evaluation metrics, tenant isolation, and authenticated human approval.

Acceptance requires Ruff, MyPy, the complete Python test suite, frontend lint, TypeScript checks,
the Next.js production build, migration SQL generation, archive integrity validation, and a live
Docker/PostgreSQL smoke test. No v0.4 tag is created until local acceptance and integration review
are complete.
