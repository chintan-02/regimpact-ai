[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "regimpact-api"
version = "0.5.0"
description = "RegImpact AI API"
requires-python = ">=3.12"
dependencies = [
  "alembic>=1.16,<2",
  "azure-identity>=1.25,<2",
  "azure-monitor-opentelemetry>=1.8,<2",
  "azure-storage-blob>=12.27,<13",
  "fastapi>=0.116,<1",
  "beautifulsoup4>=4.13,<5",
  "dramatiq[redis]>=2.0,<3",
  "httpx>=0.28,<1",
  "psycopg[binary]>=3.2,<4",
  "pydantic>=2.11,<3",
  "pydantic-settings>=2.10,<3",
  "pypdf>=6.0,<7",
  "python-multipart>=0.0.20,<1",
  "redis>=6.4,<7",
  "sqlalchemy>=2.0,<3",
  "uvicorn[standard]>=0.35,<1",
]

[project.optional-dependencies]
dev = [
  "mypy>=1.17,<2",
  "pytest>=8.4,<9",
  "reportlab>=4.4,<5",
  "ruff>=0.12,<1",
]
semantic = [
  "sentence-transformers>=5,<6",
]
ml = [
  "accelerate>=1.10,<2",
  "datasets>=4,<5",
  "numpy>=2,<3",
  "scikit-learn>=1.7,<2",
  "torch>=2.8,<3",
  "transformers>=4.56,<5",
]
notebook = [
  "ipykernel>=6.30,<7",
  "jupyterlab>=4.4,<5",
]

[tool.hatch.build.targets.wheel]
packages = ["src/regimpact"]

[tool.ruff]
line-length = 100
target-version = "py312"
